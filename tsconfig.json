{
  "compilerOptions": {
    "outDir": "./dist/",
    // не позволяет не указывать тип аргумента
    "noImplicitAny": true,
    "module": "ESNext",
    // компилируем в спецификацию es5 для поддержки большинства браузеров
    "target": "es5",
    "jsx": "react-jsx",
    // компилятор будет обрабатывать не только TS файлы, но и JS файлы
    "allowJs": true,
    "moduleResolution": "node",
    // esModuleInterop позволяет работать с пакетами, которые используют common js, как с обычными пакетами с помощью import (require() module.export) = common js
    "esModuleInterop": true,
    // позволяет использовать дефолтные импорты вместо import * as React from "react"
    "allowSyntheticDefaultImports": true,
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}